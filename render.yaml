services:
  - type: web
    name: jton-playground
    runtime: python
    plan: free
    buildCommand: pip install jton flask gunicorn tiktoken
    startCommand: gunicorn playground.app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 30
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
